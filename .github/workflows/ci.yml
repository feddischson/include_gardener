name: CI
on: [push]

jobs:
  build-gcc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - run: mkdir build
      - run: cmake -B build
      - run: make  -C build VERBOSE=1
